<?php
// Dev mode
// Comment it to turn dev mode off (or simply do not define it)
define( 'GT_DEV_MODE', true );

// Google map API key
define( 'GT_GMAP_API_KEY', 'apikey');

// GENEALOGY database connection to the plugin-specific databases
define( 'GT_DB_HOST', 'localhost' ); // Hostitel (pro localhost ponechejte "localhost")
define( 'GT_DB_DATABASE', 'bp_genealogy' ); // Název vaší databáze
define( 'GT_DB_USERNAME', 'root' ); // Uživatelské jméno (root)
define( 'GT_DB_PASSWORD', '' ); // Heslo pro root (pokud používáte heslo, vložte ho sem)

define('GT_DB_TABLE_PREFIX', 'gt_');
