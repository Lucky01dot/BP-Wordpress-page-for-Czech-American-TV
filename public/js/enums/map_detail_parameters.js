/**
 * Enum of parameters for map details that are used across the entire plugin.
 * Each parameter contains string identifier that is used in HTML selector definitions.
 * @type {{string}}
 * This enum must match with the enum defined in `includes/enums/GT_Name_Type.php` !!!
 */
const GT_NAME_DISTRIBUTION_MAP_PARAMETER = {
    LAST_NAME: "ln",
    MEP: "mep",
    REGION: "region",
};
